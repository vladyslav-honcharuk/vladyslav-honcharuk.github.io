# Radial Shell Encoding System Requirements

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Machine learning
scikit-learn>=1.0.0

# FAISS (choose one)
faiss-cpu>=1.7.0  # For CPU-only
# faiss-gpu>=1.7.0  # For GPU acceleration (uncomment if using GPU)

# Storage and compression
h5py>=3.7.0
zarr>=2.13.0
pyarrow>=10.0.0

# Progress bars
tqdm>=4.62.0

# Plotting (for lasso selection support)
matplotlib>=3.5.0

# Optional: for better performance
# numba>=0.56.0  # JIT compilation for numerical code

# Already installed (from existing xenium_processor.py)
# plotly
# geopandas (if using spatial selections)
